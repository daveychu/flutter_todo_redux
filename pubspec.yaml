name: flutter_todo_redux
description: Todo written in Flutter and Redux
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_redux: ^0.5.0
  built_value: ^6.7.0
  path_provider: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.0.0
  built_value_generator: ^6.7.0

flutter:
  uses-material-design: true
